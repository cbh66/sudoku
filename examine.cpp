#include "examine.h"
/*
int numGuesses(Guess board[][SIZE])
{
  int i, j, count = 0;
  for (i = 0; i < SIZE; i++) {
    for (j = 0; j < SIZE; j++) {
      count += board[i][j].amt;
    }
  }
  return count;
}

int numCertain(Guess board[][SIZE])
{
  int i, j, count = 0;
  for (i = 0; i < SIZE; i++) {
    for (j = 0; j < SIZE; j++) {
      if (board[i][j].amt == 1) count++;
    }
  }
  return count;
}
*/